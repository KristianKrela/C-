
#include <iostream>
using namespace std;
int main()
{
	const int dlzka_pola = 10;
	int pole[dlzka_pola];
	for (int i = 0; i < dlzka_pola; i++)
		pole[i] = i + 1;

	for (int i = 0; i < dlzka_pola; i++)
		cout << pole[i] << " ";
	cin.get(); cin.get();
	return 0;
}

